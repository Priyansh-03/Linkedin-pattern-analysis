# LinkedIn pattern analysis

Tabular + joined tables under `Dataset1/` and `Dataset2/clean_jobs.csv`.

`Dataset1/postings.csv` is not stored in this repo (GitHub file-size limits). Download from [Kaggle: LinkedIn Job Postings](https://www.kaggle.com/datasets/arshkon/linkedin-job-postings) and place it at `Dataset1/postings.csv`.

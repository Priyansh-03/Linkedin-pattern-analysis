# LinkedIn pattern analysis

Tabular + joined tables under `Dataset1/` and `Dataset2/clean_jobs.csv`.

`Dataset1/postings.csv` is not in this repo (size / optional [Git LFS](https://git-lfs.com) on GitHub). Download from [Kaggle: LinkedIn Job Postings](https://www.kaggle.com/datasets/arshkon/linkedin-job-postings) into `Dataset1/postings.csv`.
